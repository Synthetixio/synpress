{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "types/**"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "test:e2e:headless": {
      "dependsOn": ["build"]
    }
  }
}
